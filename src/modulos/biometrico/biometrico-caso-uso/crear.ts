import { Inject, Injectable, ConflictException } from '@nestjs/common';

import { plainToClass } from 'class-transformer';





@Injectable()
export class CrearBiometricoCasoUso {
  constructor(
   
  ) {}

  async crear(canton: any): Promise<any> {
    
  }
}
